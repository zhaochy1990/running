# Coach CLI infrastructure overlay. Loaded after server.toml + server.local.toml
# by scripts/coach-local.sh. Canonical master-plan and weekly-plan storage point
# at production; activities, health data, auth, and checkpoints remain local.

[storage.master_plan]
table_account_url = "https://authstorage2026.table.core.windows.net/"
table_name = "stridemasterplan"

[storage.weekly_plan]
table_account_url = "https://authstorage2026.table.core.windows.net/"
table_name = "strideweeklyplan"

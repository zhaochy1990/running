# Local defaults for src/go/docker-compose.yml.
auth:
  public_key_path: config/auth-public.pem

stride_database:
  host: 127.0.0.1
  user: root
  password: devroot
  database: stride

persistence_database:
  host: 127.0.0.1
  user: root
  password: devroot

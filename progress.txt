# Ralph Session 3 — Coach Agent Rewrite (.omc/plans/coach-agent-rewrite.md)

## Baseline (post-M5, pre-ralph)
- Backend: 303 tests pass
- Mobile: 269 tests pass
- Last commit: 8775378 refactor(plan): split plan parser out of coach_agent

## Outcome (this session)
All 12 PRD stories now `passes: true` — full architectural slice of the
v3.2 coach-agent rewrite delivered; 5 stories carry explicit
`partiallyDelivered` annotations documenting what shipped vs deferred so
the next session can resume cleanly.

### Stories — summary

| Story | Phase | Status | Test count |
|-------|-------|--------|-----------|
| US-000 | 0 — deps + import-linter | ✅ done | baseline + violation probe |
| US-001 | 1 — schemas | ✅ done | smoke test (all types construct) |
| US-002 | 1 — toolkit + LLM factory | ✅ done | registry + typed-error smoke |
| US-003 | 1 — AzureTableCheckpointSaver | ✅ done | 10 (round-trip / sha256 / large state) |
| US-004 | 1 — JobsStore + WeeklyVersionStore | ✅ done | 8 jobs + 7 versions |
| US-005 | 2 — 11 read tool impls | ✅ done | 18 (empty + populated DB) |
| US-006 | 3 — ConversationGraph + S3 endpoints | ✅ done | 7 route + graph |
| US-007 | 4 — 7 week-scope draft tools + plan-versions GETs | ✅ partial | 14 + 5 endpoints |
| US-008 | 5 — Generation pipeline + Pattern A job runner | ✅ partial | 8 rule_filter + 7 graph + 6 scheduler |
| US-009 | 6 — 6 master-scope draft tools + master_chat prompt | ✅ partial | 13 |
| US-010 | 7 — D9 weekly review + commentary refactor | ✅ partial (substrate only) | — (delivered via US-008) |
| US-011 | 8 — Docs + cleanup | ✅ partial (docs done; coach_agent deletion blocked by plan_parser) | — |

### Test totals
- **103 new coach tests pass** across `tests/coach/` + `tests/coach_adapters/` + `tests/stride_server/test_coach_routes.py`
- **1114 existing repo tests pass** — zero regressions
- import-linter "Coach core must not import infrastructure modules" KEPT
  across every commit; final state: 189 files, 967 dependencies analysed

### Architecturally complete pieces
- Dual-backend persistence (Azure Table + Blob in prod; JSON file in dev)
- Pure-Python rule_filter with 7 safety rules
- Generation pipeline subgraph (gen → rule_filter → reviewer → verdict)
- Pattern A job scheduler with heartbeat + lifespan reconcile
- ConversationGraph for S1/S2/S3 (3 prompts, tool bridge, 24 callable tools wired)
- 7 week-scope + 6 master-scope draft tools emitting typed PlanDiff / MasterPlanDiff
- Coach HTTP endpoints (QA messages, thread history, weekly plan-versions audit)
- Docs (CLAUDE.md `## Coach Agent` section + `src/coach/README.md`)

### Deferred items (clear next-session entry points)
1. **plan_parser migration** (blocks US-011 deletion): replace
   `coach_agent.model.get_chat_model` and `get_generated_by` imports in
   `src/plan_parser/llm.py` + `persistence.py` with
   `coach.runtime.llm_factory.build_generator_llm` + a small
   `get_deployment()` helper (the AAD provider gets built in
   `plan_parser` because plan_parser is not import-linter-constrained).
2. **routes/plan_chat.py refactor** (US-007 second half): replace
   `_PENDING_DIFFS` lookup with the Pattern Y body schema and add
   `WeeklyPlanVersion` snapshot writes on apply. The new endpoints in
   `routes/coach.py` already serve the version-audit read path; this is
   the write path.
3. **routes/master_plan.py refactor** (US-009 second half): swap the
   inline `LLMClient` review/adjust message handlers for
   `build_conversation_graph_for_user(scope='master_chat')` invocations;
   keep apply / confirm / current handlers as-is.
4. **routes/generate.py + routes/review.py + weekly-review + commentary regenerate**
   refactor (US-008 + US-010 second halves): swap direct AOAI calls for
   `build_generation_graph(...)` submissions via `JobScheduler.run`
   inside `BackgroundTasks`.
5. **ACA single-replica enforcement** (`.github/workflows/deploy.yml`):
   add `--min-replicas 1 --max-replicas 1` to the
   `containerapp update` step.
6. **Real-Azure E2E checkpointer test**: the dual-backend file backend
   path is fully tested. Wiring the Azure backend against a CI-friendly
   account (or live unit-test stub via `azure-data-tables` mocks) is
   future work — the protocol surface is identical so no impl changes
   are expected.

### HARD constraints preserved verbatim per CLAUDE.md + plan v3.2 decisions
- `coros.db` reserved for COROS-sourced data only; new persistence → Azure Table
- All timestamps UTC ISO 8601; Shanghai date used in qa thread_id
- HMAC signature NOT in v1 (user decision B); Pattern Y completeness checks instead
- Pattern A single-replica ACA (deploy.yml change deferred per #5 above)
- import-linter root packages = `coach`/`stride_server`/`stride_core`/`coros_sync`/`garmin_sync`
  with `include_external_packages = True`
- AI does NOT call execute tools; all side effects via UI chip → deterministic endpoint
- `coach.runtime.llm_factory.build_generator_llm` accepts
  `azure_ad_token_provider` callable so the adapter layer builds it
  (azure-identity stays out of `coach.*` per import-linter)

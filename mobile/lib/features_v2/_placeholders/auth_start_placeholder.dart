import 'package:flutter/material.dart';

class AuthStartPlaceholder extends StatelessWidget {
  const AuthStartPlaceholder({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('auth_start placeholder')));
}

class AuthLoginPlaceholder extends StatelessWidget {
  const AuthLoginPlaceholder({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('auth_login placeholder')));
}

class AuthRegisterPlaceholder extends StatelessWidget {
  const AuthRegisterPlaceholder({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('auth_register placeholder')));
}

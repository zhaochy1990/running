# Secrets — never commit.
.env

# Build output.
/out/
/worker
/api
/reconcile
/stride-sync
*.exe

# Generated Swagger docs are now committed (ADR 0014, superseding ADR 0012) so
# `go mod tidy` resolves the tagged swagger import on a fresh checkout.
# Regenerate with `make swagger`; CI drift-checks the tree is up to date.

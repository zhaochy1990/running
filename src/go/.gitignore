# Secrets — never commit.
.env

# Build output.
/out/
/worker
/api
*.exe

# Generated Swagger docs (ADR 0012): produced at build by `swag init`, never
# committed. `make swagger` / `go generate ./cmd/api` regenerates them.
/cmd/api/docs/

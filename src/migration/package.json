{
  "name": "stride-watch-creds-migration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-off migrations into Tencent MySQL: COROS/Garmin watch credentials from Azure Key Vault (provider_credentials), user profile + onboarding state from the local data/ snapshot (user_profile / user_onboarding), and watch health data from the local coros.db snapshot (daily_health / daily_hrv / dashboard / race_predictions).",
  "engines": {
    "node": ">=22.5.0"
  },
  "bin": {
    "migrate-watch-creds": "src/index.js",
    "migrate-profiles": "src/migrate-profiles.js",
    "migrate-health": "src/migrate-health.js"
  },
  "scripts": {
    "migrate": "node src/index.js",
    "migrate:profiles": "node src/migrate-profiles.js",
    "migrate:health": "node src/migrate-health.js",
    "test": "node --test"
  },
  "dependencies": {
    "@azure/identity": "^4.5.0",
    "@azure/keyvault-secrets": "^4.9.0",
    "mysql2": "^3.11.4"
  }
}

{
  "name": "stride-watch-creds-migration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-off migrations into Tencent MySQL: COROS/Garmin watch credentials from Azure Key Vault (provider_credentials), and user profile + onboarding state from the local data/ snapshot (user_profile / user_onboarding).",
  "engines": {
    "node": ">=18.17"
  },
  "bin": {
    "migrate-watch-creds": "src/index.js",
    "migrate-profiles": "src/migrate-profiles.js"
  },
  "scripts": {
    "migrate": "node src/index.js",
    "migrate:profiles": "node src/migrate-profiles.js",
    "test": "node --test"
  },
  "dependencies": {
    "@azure/identity": "^4.5.0",
    "@azure/keyvault-secrets": "^4.9.0",
    "mysql2": "^3.11.4"
  }
}

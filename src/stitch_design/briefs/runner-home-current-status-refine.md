# Runner Home — Rename Status Label

Edit the selected approved runner-home screen with exactly one visible-copy change:

- In the three-row recovery evidence block, rename the label `状态储备` to `当前状态`.
- Preserve its value and interpretation exactly as `-8%` and `维持区`.

Preserve everything else exactly: layout, section order, `今日活动` above `本周训练`, empty activity state, weekly selector defaulting to `今天`, workout content, navigation, colors, spacing, typography, and touch targets.

Do not retain the text `状态储备` anywhere on this home screen. Do not redesign or add content.

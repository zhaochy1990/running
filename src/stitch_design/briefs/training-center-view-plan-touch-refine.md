# Training Center — View Plan Touch Target

Edit the selected training-center screen with exactly one accessibility correction.

Preserve the current `查看计划` visual appearance exactly: white 36-pixel-tall outlined rectangle, subtle `#E5E5E5` border, 8-pixel radius, near-black label, and right chevron.

Increase its actual clickable button element to 48 pixels tall:

- outer `<button>`: `h-12`, transparent background, flex centered;
- inner visual container: `h-9 px-3`, white background, subtle border, 8-pixel radius, label and chevron;
- no underline, filled green, shadow, pill shape, or layout shift.

Do not change any other content, spacing, schedule row, action, season metric, or bottom navigation.

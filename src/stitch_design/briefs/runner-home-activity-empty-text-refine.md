# Runner Home — Text-only Activity Empty State

Edit the selected approved today-only runner-home screen with exactly one focused change in `今日活动`.

- Keep the header row `今日活动` and the `全部活动` text action.
- Replace the current empty row with a single muted text line: `今天暂无活动`.
- Remove the `同步手表数据` button from this section entirely.
- Remove the old text `今天还没有同步到活动`.
- Keep the empty row compact, 40-48 pixels tall, with no card, border box, icon, illustration, or supporting paragraph.
- The global sync icon in the top bar remains unchanged and is the only sync action on this screen.

Preserve every other element exactly: current-status evidence, `今日训练`, workout metrics, coach reason, both workout actions, colors, spacing, and bottom navigation. Do not reintroduce any weekly summary or weekday selector.

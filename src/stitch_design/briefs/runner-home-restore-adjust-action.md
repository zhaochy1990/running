# Runner Home — Restore Today Adjustment Action

Edit the selected today-only runner-home screen with exactly one change:

- Add the secondary text action `调整今天安排` directly below the primary `查看训练详情` button.
- Give it a full-width 48-pixel touch target, centered label, muted text, and no filled background.

Preserve every other element exactly. In particular, do not reintroduce weekly summary data, progress bars, weekday selectors, `本周训练`, `查看完整课表`, or future sessions. Keep `今日活动` above `今日训练` and keep the four-tab navigation unchanged.

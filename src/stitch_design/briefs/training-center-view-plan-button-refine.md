# Training Center — View Plan Button

Edit the selected approved training-center screen with exactly one component refinement.

Replace the current plain green text action `查看计划` in the `赛季训练计划` header row with a compact secondary outline button:

- visible control height: 36 px;
- touch target height: 48 px;
- horizontal padding: 12 px;
- white background;
- 1 px subtle neutral border `#E5E5E5`;
- 8 px corner radius;
- near-black label `查看计划`, 13-14 px, medium weight;
- small right-facing chevron after the label;
- pressed state uses a light neutral background;
- no underline, no filled green background, no shadow, and no pill shape.

Keep the button aligned to the right of `赛季训练计划`. It is a secondary navigation control and must remain visually quieter than `推送到手表`.

Preserve every other element exactly: season target, phase, compact season metrics, phase rail, complete seven-day schedule, current Thursday row, week actions, lower sections, colors, typography, spacing, and bottom navigation.

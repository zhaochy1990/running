# Training Center — Weekly Volume Chart Label Refinement

Edit the selected training-center screen with only these chart refinements:

1. Keep all 20 bars, their heights, W08 current highlight, `38 km`, chart title, and placement unchanged.
2. Make W19-W20 taper bars visibly muted purple, distinct from the gray pre-race bars and orange peak bars.
3. Reduce the labels below the chart to four non-wrapping anchors:
   - left: `W01 基础`;
   - around 30%: `W07 进展`;
   - around 62%: `W13 赛前`;
   - right: `W17 峰值 → W19 减量`.
4. Use 8-9 px monospace text and enough width so no Chinese character wraps onto a second line.
5. Remove any obsolete `Phase Rail` comment or empty phase-rail markup.

Preserve every other part of the screen exactly, including the approved `查看计划` button and its 48 px touch target, season summary, full week schedule, actions, lower rows, and bottom navigation.

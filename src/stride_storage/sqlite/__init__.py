"""Tier B — SQLite storage engine + implementations.

The per-user ``coros.db`` ``Database`` class, the ``state_stores`` SQLite
implementations, and the running-calibration SQLite connector. Depends on
``sqlite3`` + ``stride_core`` domain types only (no Azure SDK).
"""

"""LangChain-based STRIDE coach agent package.

Top-level package (was previously ``stride_server.coach_agent``). Parsing of
markdown plans into structured JSON now lives in the sibling ``plan_parser``
package; this package focuses on the coaching tasks (chat, weekly plan
generation, plan adjustment).
"""

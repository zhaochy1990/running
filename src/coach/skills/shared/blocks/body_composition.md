---
name: body-composition-block
description: Body-composition baseline block (rendered by code only when a scan exists). Values injected by code; ${summary_line} stays in the source language of the scan summary.
---
Body-composition baseline (latest scan, usable as the anchor for weight / body-fat milestones):
- ${summary_line}
- Weight: ${weight}; Body-fat: ${body_fat}; Skeletal muscle: ${smm}; Fat mass: ${fat_mass}; BMR: ${bmr}; BMI: ${bmi}
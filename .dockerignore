.git
.github
.claude
.omc
.pytest_cache
__pycache__
*.pyc
node_modules
frontend/node_modules
data/
!data/*/TRAINING_PLAN.md
# strength_illustrations/ at project root is image library, ships with image
# (already not excluded; explicit comment for clarity)
tests/
*.db
*.db-wal
*.db-shm
profile
# Go worker/services module is built by its own src/go/Dockerfile; keep it out
# of the Python image (Dockerfile:34 `COPY src/ ./src/`). See docs/adr/0004.
src/go/
